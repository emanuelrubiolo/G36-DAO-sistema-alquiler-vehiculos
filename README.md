# Proyecto: Sistema de Alquiler de Vehículos

## Ejecutar tests

Recomendado: crear un entorno virtual e instalar dependencias.

PowerShell (Windows):

```powershell
cd C:/Users/cti24370/Desktop/GitHub/G36-DAO-sistema-alquiler-vehiculos
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
pytest -q
```

Esto instalará `pytest` (desde `requirements.txt`) y ejecutará la suite de pruebas.
# .

